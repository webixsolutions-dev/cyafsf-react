import { describe, it, expect } from 'vitest';

// Placeholder — wire up @testing-library/react to render <App /> inside
// a MemoryRouter once component work begins.
describe('App', () => {
  it('has a test scaffold ready', () => {
    expect(true).toBe(true);
  });
});
