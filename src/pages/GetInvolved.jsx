export default function GetInvolved() {
  return (
    <section className="container py-16">
      <h1 className="mb-3">GetInvolved</h1>
      <p className="max-w-[62ch] text-ink">
        Port the corresponding section from the static-site build
        (<code>cyafsf-static-site/get-involved.html</code>) into this component,
        using the same Tailwind utility patterns as <code>Home.jsx</code>.
      </p>
    </section>
  );
}
